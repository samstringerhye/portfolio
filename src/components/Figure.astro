---
interface Props {
  caption?: string
  flush?: boolean
}

const { caption, flush = false } = Astro.props
---

<figure class:list={['cs-figure', { 'cs-figure--flush': flush }]}>
  <slot />
  {caption && <figcaption>{caption}</figcaption>}
</figure>
