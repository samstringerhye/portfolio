---
/**
 * Motion System diagram for Whataburger case study.
 * 3 duration tiers + easing curve with interactive demos.
 * Animated dots play the actual duration on hover.
 * Easing curve has a traveling dot on click.
 */
const durations = [
  { name: 'Fast', ms: 100, usage: 'Toggles, checkboxes', width: '20%' },
  { name: 'Normal', ms: 150, usage: 'Sheets, modals', width: '40%' },
  { name: 'Slow', ms: 300, usage: 'Page transitions', width: '80%' },
]
---

<section class="motion-system" aria-label="Motion system">
  <div class="motion-grid">
    <div class="motion-durations">
      <h4 class="motion-section-title">Duration</h4>
      <div class="duration-bars">
        {durations.map(d => (
          <div class="duration-item" data-duration={d.ms} tabindex="0" role="button">
            <div class="duration-bar-track">
              <div class="duration-bar-fill" style={`width: ${d.width};`} />
              <div class="duration-dot" aria-hidden="true" />
            </div>
            <div class="duration-meta">
              <span class="duration-name">{d.name}</span>
              <span class="duration-ms">{d.ms}ms</span>
            </div>
            <span class="duration-usage">{d.usage}</span>
          </div>
        ))}
      </div>
      <p class="motion-hint">Hover a bar to see the timing</p>
    </div>

    <div class="motion-easing">
      <h4 class="motion-section-title">Easing</h4>
      <div class="easing-card">
        <div class="easing-label">ease-in-out</div>
        <p class="easing-desc">Standard easing for all transitions.<br />Smooth acceleration and deceleration.</p>
        <div class="easing-demo">
          <svg class="easing-curve" viewBox="0 0 200 200" fill="none" aria-label="Cubic bezier curve: 0.42, 0, 0.58, 1">
            <rect x="0" y="0" width="200" height="200" fill="none" stroke="var(--wab-border)" stroke-width="1" rx="4" />
            <line x1="0" y1="200" x2="200" y2="0" stroke="var(--wab-border)" stroke-width="1" stroke-dasharray="4 4" />
            <!-- Control point lines -->
            <line x1="0" y1="200" x2="84" y2="200" stroke="var(--wab-highlight)" stroke-width="1" />
            <line x1="200" y1="0" x2="116" y2="0" stroke="var(--wab-highlight)" stroke-width="1" />
            <!-- Curve -->
            <path class="easing-path" d="M 0 200 C 84 200, 116 0, 200 0" stroke="var(--wab-orange)" stroke-width="2.5" fill="none" />
            <!-- Control points -->
            <circle cx="84" cy="200" r="4" fill="var(--wab-orange)" />
            <circle cx="116" cy="0" r="4" fill="var(--wab-orange)" />
            <!-- Traveling dot -->
            <circle class="easing-dot" cx="0" cy="200" r="6" fill="var(--wab-brown)" opacity="0" />
            <!-- Labels -->
            <text x="84" y="192" font-size="10" fill="var(--wab-muted)" text-anchor="middle">P1 (0.42, 0)</text>
            <text x="116" y="16" font-size="10" fill="var(--wab-muted)" text-anchor="middle">P2 (0.58, 1)</text>
          </svg>
          <button class="easing-play" aria-label="Play easing animation" type="button">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
              <polygon points="5,3 19,12 5,21" />
            </svg>
            <span>Play</span>
          </button>
        </div>
        <code class="easing-code">cubic-bezier(0.42, 0, 0.58, 1)</code>
      </div>
    </div>
  </div>
</section>

<style>
  .motion-system {
    --wab-bg: #fdf8f4;
    --wab-bg-alt: #f4ebe1;
    --wab-brown: #472c19;
    --wab-muted: #806c5b;
    --wab-muted-alt: #a0846c;
    --wab-orange: #fe7800;
    --wab-border: #f4ebe1;
    --wab-highlight: #ffd6ba;
    --wab-muted-mid: #948274;
    --wab-tan: #cdb9a7;
    background: var(--wab-bg);
    border-radius: 0;
    border-top: var(--border-width-1) solid var(--color-text-primary);
    border-bottom: var(--border-width-1) solid var(--color-text-primary);
    padding: 2rem;
    margin: 2rem 0;
  }

  .motion-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }

  .motion-section-title {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--wab-brown);
    margin: 0 0 1rem;
  }

  .duration-bars {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .duration-item {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    cursor: pointer;
  }

  .duration-bar-track {
    height: 8px;
    background: color-mix(in srgb, var(--wab-highlight) 30%, transparent);
    border-radius: 4px;
    overflow: hidden;
    position: relative;
  }

  .duration-bar-fill {
    height: 100%;
    background: var(--wab-orange);
    border-radius: 4px;
  }

  .duration-dot {
    position: absolute;
    top: 50%;
    left: 0;
    width: 12px;
    height: 12px;
    background: var(--wab-brown);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
    pointer-events: none;
  }

  .duration-meta {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
  }

  .duration-name {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--wab-brown);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .duration-ms {
    font-family: 'SF Mono', 'Fira Code', monospace;
    font-size: 0.78rem;
    color: var(--wab-muted);
  }

  .duration-usage {
    font-size: 0.72rem;
    color: var(--wab-muted-mid);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .motion-hint {
    font-size: 0.68rem;
    color: var(--wab-muted-alt);
    margin: 0.75rem 0 0;
    font-style: italic;
  }

  .easing-card {
    background: white;
    border-radius: 12px;
    padding: 1.25rem;
  }

  .easing-label {
    font-size: 1rem;
    font-weight: 700;
    color: var(--wab-brown);
    margin-bottom: 0.35rem;
  }

  .easing-desc {
    font-size: 0.78rem;
    color: var(--wab-muted);
    margin: 0 0 1rem;
    line-height: 1.5;
  }

  .easing-demo {
    display: flex;
    align-items: flex-end;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .easing-curve {
    width: 100%;
    max-width: 200px;
    height: auto;
  }

  .easing-play {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    background: var(--wab-bg);
    border: 1px solid var(--wab-border);
    border-radius: 6px;
    padding: 0.4rem 0.65rem;
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--wab-brown);
    cursor: pointer;
    transition: background 0.15s ease, border-color 0.15s ease;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .easing-play:hover {
    background: var(--wab-bg-alt);
    border-color: var(--wab-tan);
  }

  .easing-play svg {
    color: var(--wab-orange);
  }

  .easing-code {
    display: block;
    font-family: 'SF Mono', 'Fira Code', monospace;
    font-size: 0.75rem;
    color: var(--wab-muted);
  }

  .duration-item:focus-visible,
  .easing-play:focus-visible {
    outline: 2px solid var(--wab-orange);
    outline-offset: 2px;
  }

  @media (max-width: 768px) {
    .motion-grid {
      grid-template-columns: 1fr;
    }
    .motion-system {
      padding: 1.25rem;
    }
    .motion-hint {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .duration-dot,
    .easing-dot,
    .easing-play,
    .motion-hint {
      display: none;
    }
  }
</style>

<script>
  let ctx: { revert: () => void } | null = null

  async function initMotionSystem() {
    const container = document.querySelector('.motion-system') as HTMLElement | null
    if (!container || container.dataset.motionInit === 'true') return
    container.dataset.motionInit = 'true'

    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return

    const { default: gsap } = await import('gsap')
    const cleanups: (() => void)[] = []

    ctx?.revert()
    ctx = gsap.context(() => {

    // Duration bar hover demos — animated dot travels the bar width
    const items = container.querySelectorAll<HTMLElement>('.duration-item')
    items.forEach(item => {
      const ms = parseInt(item.dataset.duration || '150', 10)
      const dot = item.querySelector<HTMLElement>('.duration-dot')
      const fill = item.querySelector<HTMLElement>('.duration-bar-fill')
      if (!dot || !fill) return

      const playDemo = () => {
        const fillWidth = fill.offsetWidth
        gsap.killTweensOf(dot)
        gsap.set(dot, { left: 0, opacity: 1 })
        gsap.to(dot, {
          left: fillWidth,
          duration: ms / 1000,
          ease: 'power2.inOut',
          onComplete() {
            gsap.to(dot, { opacity: 0, duration: 0.2, delay: 0.3 })
          }
        })
      }

      const handleKeydown = (event: KeyboardEvent) => {
        if (event.key === 'Enter' || event.key === ' ') {
          event.preventDefault()
          playDemo()
        }
      }

      item.addEventListener('mouseenter', playDemo)
      item.addEventListener('click', playDemo)
      item.addEventListener('keydown', handleKeydown)
      cleanups.push(() => {
        item.removeEventListener('mouseenter', playDemo)
        item.removeEventListener('click', playDemo)
        item.removeEventListener('keydown', handleKeydown)
        gsap.killTweensOf(dot)
      })
    })

    // Easing curve play button — dot traces the bezier path
    const playBtn = container.querySelector<HTMLElement>('.easing-play')
    const easingDot = container.querySelector<SVGCircleElement>('.easing-dot')
      const easingPath = container.querySelector<SVGPathElement>('.easing-path')

      if (playBtn && easingDot && easingPath) {
        let easingProgress: { t: number } | null = null

        const playEasing = () => {
        const length = easingPath.getTotalLength()
        gsap.killTweensOf(easingDot)
        gsap.set(easingDot, { opacity: 1 })

          easingProgress = { t: 0 }
          gsap.to(easingProgress, {
            t: 1,
          duration: 1.2,
          ease: 'none',
          onUpdate() {
              const point = easingPath.getPointAtLength((easingProgress?.t || 0) * length)
            easingDot.setAttribute('cx', String(point.x))
            easingDot.setAttribute('cy', String(point.y))
          },
          onComplete() {
            gsap.to(easingDot, { opacity: 0, duration: 0.3, delay: 0.5 })
          }
        })
      }

        playBtn.addEventListener('click', playEasing)
        cleanups.push(() => {
          playBtn.removeEventListener('click', playEasing)
          gsap.killTweensOf(easingDot)
          if (easingProgress) gsap.killTweensOf(easingProgress)
        })
    }
    }, container)

    document.addEventListener('astro:before-swap', () => {
      cleanups.forEach(remove => remove())
      ctx?.revert()
      ctx = null
    }, { once: true })
  }

  initMotionSystem()
  document.addEventListener('astro:page-load', initMotionSystem)
</script>
