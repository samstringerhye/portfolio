---
import { Icon } from 'astro-icon/components'
import couplePhoto from '../../assets/work/amica-design-system/bento/photo-couple-fence.jpg'
import roadAheadPhoto from '../../assets/work/amica-design-system/bento/car-road-ahead-masthead.jpg'
import avatarImage from '../../assets/work/amica-design-system/bento/avatar.jpg'
import logo from '../../assets/work/amica-design-system/bento/amica-logo.svg?raw'
import trustpilotStar from '../../assets/work/amica-design-system/hero/trustpilot-star.svg?raw'
import productAuto from '../../assets/work/amica-design-system/bento/product-auto.svg?raw'
import productHome from '../../assets/work/amica-design-system/bento/product-home.svg?raw'
import productCondo from '../../assets/work/amica-design-system/bento/product-condo.svg?raw'
import productRenters from '../../assets/work/amica-design-system/bento/product-renters.svg?raw'
import productGridAuto from '../../assets/work/amica-design-system/bento/product-grid-auto.jpg'
import productGridMotorcycle from '../../assets/work/amica-design-system/bento/product-grid-motorcycle.jpg'
import productGridMarine from '../../assets/work/amica-design-system/bento/product-grid-marine.jpg'
import productGridRv from '../../assets/work/amica-design-system/bento/product-grid-rv.jpg'
import productGridBundle from '../../assets/work/amica-design-system/bento/product-grid-bundle.jpg'

const preserveIllustrationRatio = (svg: string) =>
  svg.replace('preserveAspectRatio="none"', 'preserveAspectRatio="xMidYMid meet"')

const products = [
  { name: 'Auto', icon: preserveIllustrationRatio(productAuto), selected: true },
  { name: 'Home', icon: preserveIllustrationRatio(productHome) },
  { name: 'Condo', icon: preserveIllustrationRatio(productCondo) },
  { name: 'Renters', icon: preserveIllustrationRatio(productRenters) },
]

const editorialCards = [
  {
    title: 'Home, made personal',
    body: 'Coverage shaped around the way you live.',
    label: 'Home insurance',
    image: '/_protected-embeds/amica-prototype/images/June%2010%20Prototype_img/Home_EverythingInOrder_0684_4666EHG_C_ADOBERGB.jpg',
  },
  {
    title: 'Ready for the road',
    body: 'Protection that stays with you from driveway to destination.',
    label: 'Auto insurance',
    image: '/_protected-embeds/amica-prototype/images/June%2010%20Prototype_img/woman-with-motorcycle-orig.jpg',
  },
  {
    title: 'Help when it matters',
    body: 'Real guidance from people who know your coverage.',
    label: 'Our service',
    image: '/_protected-embeds/amica-prototype/images/sections/care.jpg',
  },
]

const quickLinkGroups = [
  {
    label: 'Log in',
    links: [
      { label: 'Pay A Bill', icon: 'material-symbols:paid-outline' },
      { label: 'Report A Claim', icon: 'material-symbols:error-outline' },
      { label: 'Log In', icon: 'material-symbols:person-outline' },
    ],
  },
  {
    label: 'No sign in required',
    links: [
      { label: 'Pay As Guest', icon: 'material-symbols:paid-outline' },
      { label: 'Report Any Claim', icon: 'material-symbols:error-outline' },
      { label: 'Create Account', icon: 'material-symbols:person-outline' },
    ],
  },
]

const productGridCards = [
  { name: 'Auto', image: productGridAuto },
  { name: 'Motorcycle', image: productGridMotorcycle },
  { name: 'Marine', image: productGridMarine },
  { name: 'RV & Trailer', image: productGridRv },
  { name: 'Bundle & Save', image: productGridBundle, offer: true },
]

const statCards = [
  { value: '97', suffix: '%', label: 'Claims resolved in one call' },
  { value: '94', suffix: '%', label: 'Members who’d recommend Amica' },
  { value: '91', suffix: '%', label: 'Policyholder renewal rate' },
]

const chevron = '<svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="m9 6 6 6-6 6" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></svg>'
const down = '<svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="m6 9 6 6 6-6" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></svg>'
---

<section class="mosaic" aria-label="Amica design system in use — component mosaic">
  <div class="mosaic__grid">
    <article class="tile tile--masthead">
      <img class="mosaic-media masthead-image" src={roadAheadPhoto.src} alt="Couple standing beside their car at sunset" />
      <div class="masthead-card">
        <div class="masthead-rating" aria-label="Excellent rating on Trustpilot">
          <strong>Excellent</strong>
          <span class="masthead-stars" aria-hidden="true">
            {Array.from({ length: 5 }, () => <i set:html={trustpilotStar} />)}
          </span>
          <span>Trustpilot</span>
        </div>
        <h2>Car insurance that's made for your road ahead</h2>
        <p>Protection and support built around you.</p>
        <div class="masthead-fields" aria-hidden="true">
          <span>Auto <i set:html={down} /></span>
          <span class="masthead-zip">ZIP code</span>
        </div>
        <span class="button button--full">Get a quote</span>
        <span class="masthead-link">Continue a saved quote</span>
      </div>
    </article>

    <div class="tile tile--photo"><img class="mosaic-media" src={couplePhoto.src} alt="Couple leaning on a fence outside their home" /></div>

    <article class="tile tile--editorial" aria-label="Explore Amica coverage">
      <div class="editorial-panel">
        <header class="editorial-header">
          <span>Insurance for real life</span>
          <h3>Coverage for every part of your world</h3>
          <p>Straightforward protection, backed by people who show up.</p>
        </header>
        <div class="editorial-list">
          {editorialCards.map((card) => (
            <div class="editorial-item">
              <img class="mosaic-media" src={card.image} alt="" />
              <span class="editorial-item__copy">
                <strong>{card.title}</strong>
                <span class="editorial-item__body">{card.body}</span>
                <span class="editorial-item__link">{card.label}</span>
              </span>
            </div>
          ))}
        </div>
        <div class="editorial-button">Explore coverage</div>
      </div>
    </article>

    <article class="tile tile--product-grid" aria-label="Insurance products">
      <header class="product-grid__header">
        <h3>Insurance built for real people, by real people.</h3>
        <p>No two policies are the same. Just as it should be</p>
      </header>
      <div class="product-grid__tabs" aria-hidden="true">
        {['Vehicles', 'Property', 'Personal', 'Small Business', 'Retirement', 'Bundle & Save'].map((tab, index) => <span class:list={{ active: index === 0 }}>{tab}</span>)}
      </div>
      <div class="product-grid__cards">
        {productGridCards.map((card) => (
          <figure class="product-grid__card">
            {card.offer && <span class="product-grid__offer">Offer</span>}
            <img class="mosaic-media" src={card.image.src} alt="" />
            <figcaption>{card.name}</figcaption>
            <span class="product-grid__arrow" aria-hidden="true">→</span>
          </figure>
        ))}
      </div>
      <div class="product-grid__button">Explore All Products</div>
    </article>

    <article class="tile tile--stats" aria-label="Reasons to believe statistics">
      <header class="stats-header">
        <h3>Coverage you can actually trust</h3>
      </header>
      <div class="stats-card">
        <div class="stats-card__group">
          {statCards.map((stat) => (
            <div class="stats-card__stat">
              <strong>{stat.value}<small>{stat.suffix}</small></strong>
              <span>{stat.label}</span>
            </div>
          ))}
        </div>
      </div>
    </article>

    <article class="tile tile--hero-copy">
      <div class="hero-copy">
        <div class="hero-top"><span class="logo" set:html={logo} /><span class="login">Log In</span></div>
        <div class="hero-content"><h2>Insurance that protects <em>everything worth protecting</em></h2><p>Real coverage from real people, since 1907.</p><span class="button">Get A Quote</span></div>
      </div>
    </article>

    <article class="tile tile--quick-links" aria-label="Account quick links">
      {quickLinkGroups.map((group) => (
        <section class="quick-link-group" aria-label={group.label}>
          <h3>{group.label}</h3>
          <div class="quick-link-row">
            {group.links.map((link) => (
              <div class="quick-link-item">
                <Icon name={link.icon} aria-hidden="true" />
                <span>{link.label}</span>
              </div>
            ))}
          </div>
        </section>
      ))}
    </article>

    <article class="tile tile--faq">
      {['How do I file a claim?', 'What does home insurance cover?', 'Can I bundle auto and home?', 'How are dividends paid?'].map((question) => <div class="faq-row"><span>{question}</span><i set:html={chevron} /></div>)}
    </article>

    <article class="tile tile--nav">
      <div class="nav-card">{['Insurance', 'Claims', 'Customer Service', 'Resources', 'Find an office'].map((item) => <div class="nav-row"><span>{item}</span><i>→</i></div>)}</div>
    </article>

    <article class="tile tile--macro" aria-hidden="true"><span class="macro-ring"><span>Get A Quote</span></span></article>

    <article class="tile tile--picker">
      <div class="picker-shell">
        <h3>What would you like to insure?</h3>
        <div class="picker-row">{products.map((product) => <div class:list={['product-card', { 'product-card--selected': product.selected }]} data-product={product.name.toLowerCase()}><i class="checkbox" aria-hidden="true">{product.selected && '✓'}</i><span class="product-illustration" set:html={product.icon} /><span class="product-name">{product.name}</span></div>)}</div>
      </div>
    </article>

    <article class="tile tile--testimonial">
      <span class="quote-mark">"</span>
      <p>After the storm, Amica <em>called us before we even called them.</em> Everything was handled in days.</p>
      <div class="attribution-row">
        <img src={avatarImage.src} alt="" class="avatar mosaic-media" />
        <div class="attribution-info">
          <div class="attribution-name">M. Delgado</div>
          <div class="attribution-subtext">Policyholder since 2011</div>
        </div>
      </div>
    </article>
  </div>
</section>

<script>
  const video = document.currentScript?.closest('.mosaic')?.querySelector('video')
  if (video && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    video.removeAttribute('autoplay')
    video.pause()
  }
</script>

<style is:global>
  @font-face {
    font-family: 'Apercu Pro';
    src: url('/_protected-embeds/amica-prototype/fonts/Apercu-Regular-Pro.otf') format('opentype');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Apercu Pro';
    src: url('/_protected-embeds/amica-prototype/fonts/Apercu-Bold-Pro.otf') format('opentype');
    font-weight: 700;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Amica Rhymes';
    src: url('/_protected-embeds/amica-prototype/fonts/AmicaRhymes-Regular.otf') format('opentype');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Amica Rhymes';
    src: url('/_protected-embeds/amica-prototype/fonts/AmicaRhymes-Medium.otf') format('opentype');
    font-weight: 500;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Amica Rhymes';
    src: url('/_protected-embeds/amica-prototype/fonts/AmicaRhymes-Italic.otf') format('opentype');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }
</style>

<style>
  .mosaic { --teal:#00a88f; --forest:#003d33; --mint:#d6fcf5; --tint:#effffc; --ink:#001512; --cream:#f7f4e8; --tile-padding-standard:clamp(16px,1.67vw,24px); --tile-padding-compact:clamp(10px,1.11vw,16px); font-family:"Apercu Pro",var(--font-primary,system-ui); }
  .mosaic__grid { display:grid; grid-template-columns:repeat(12,minmax(0,1fr)); grid-template-rows:repeat(3,1fr); gap:1px; background:#001512; aspect-ratio:2 / 1; }
  .tile { position:relative; min-width:0; min-height:0; overflow:hidden; color:var(--ink); }
  .tile--masthead{grid-column:1/3;grid-row:1;background:var(--forest)}
  .masthead-image{display:block;width:100%;height:100%;object-fit:cover;object-position:59% center}
  .tile--masthead::after{content:"";position:absolute;inset:0;background:linear-gradient(90deg,rgba(0,21,18,.12),transparent 66%);pointer-events:none}
  .masthead-card{position:absolute;z-index:1;top:var(--tile-padding-compact);left:var(--tile-padding-compact);bottom:calc(var(--tile-padding-standard) * -2.5);width:66%;box-sizing:border-box;border-radius:clamp(8px,.83vw,12px);background:#fff;padding:var(--tile-padding-compact);box-shadow:0 12px 32px rgba(0,21,18,.18);display:flex;flex-direction:column;align-items:flex-start;gap:clamp(5px,.49vw,7px)}
  .masthead-rating{display:flex;align-items:center;gap:clamp(3px,.28vw,4px);font-size:clamp(7px,.65vw,9px);line-height:1;color:#24332f;white-space:nowrap}.masthead-rating strong{font-weight:700}.masthead-stars{display:flex;gap:1px}.masthead-stars i{display:block;width:clamp(7px,.69vw,10px);height:clamp(7px,.69vw,10px)}.masthead-stars i :global(svg){display:block;width:100%;height:100%}
  .masthead-card h2{margin:0;max-width:12ch;font:400 clamp(16px,1.67vw,24px)/1.02 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.03em}.masthead-card p{margin:0;max-width:26ch;font-size:clamp(6px,.56vw,8px);line-height:1.25;color:#50635e}
  .masthead-fields{display:flex;width:100%;gap:clamp(4px,.42vw,6px);margin-top:clamp(1px,.14vw,2px)}.masthead-fields>span{height:clamp(22px,2.08vw,30px);min-width:0;flex:1;border:1px solid #c7d8d3;border-radius:clamp(4px,.42vw,6px);padding:0 clamp(6px,.56vw,8px);display:flex;align-items:center;justify-content:space-between;font-size:clamp(6px,.56vw,8px);color:#23423b}.masthead-fields i{display:flex;width:clamp(8px,.83vw,12px)}.masthead-fields i :global(svg){width:100%}.masthead-fields .masthead-zip{color:#80908c;justify-content:flex-start}
  .masthead-card .button--full{height:clamp(24px,2.22vw,32px);border-radius:clamp(4px,.42vw,6px);font-size:clamp(6px,.63vw,9px)}.masthead-link{font-size:clamp(7px,.65vw,9px);line-height:1;color:#005c47;text-decoration:underline;text-underline-offset:2px}
  .button{display:inline-flex;align-items:center;justify-content:center;background:#00a88f;color:#000000;border-radius:9px;padding:0 18px;height:42px;font-size:13.5px;font-weight:400;white-space:nowrap;font-family:"Apercu Pro",var(--font-primary,system-ui)}.button--small{height:30px;border-radius:6px;padding:0 16px;font-size:11px}.button--full{width:100%;height:36px;border-radius:6px;font-size:12.5px}
  .tile--photo{grid-column:3/7;grid-row:1}.tile--photo img{width:100%;height:100%;display:block;object-fit:cover}
  .tile--quick-links{grid-column:1/3;grid-row:1;box-sizing:border-box;padding:clamp(12px,6cqi,20px);background:#fff;box-shadow:inset 0 -8px 12px -12px rgba(0,168,143,.65);display:flex;flex-direction:column;container-type:inline-size}.quick-link-group{min-width:0;min-height:0;flex:1;display:flex;flex-direction:column;justify-content:center}.quick-link-group+ .quick-link-group{border-top:1px solid #dfe7e4}.quick-link-group h3{margin:0 0 clamp(9px,4.5cqi,16px);font:400 clamp(8px,3.8cqi,13px)/1.1 var(--font-mono,ui-monospace,SFMono-Regular,Menlo,monospace);letter-spacing:.03em;text-transform:uppercase;color:#69716f}.quick-link-row{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(5px,2.5cqi,10px)}.quick-link-item{min-width:0;color:#006b58;display:flex;flex-direction:column;align-items:center;gap:clamp(4px,2cqi,7px);font-size:clamp(8px,4cqi,14px);line-height:1.15;text-align:center}.quick-link-item :global(svg){width:clamp(17px,7.5cqi,24px);height:clamp(17px,7.5cqi,24px);color:#006b58}.quick-link-item span{display:block;max-width:9ch}
  .tile--editorial{grid-column:7/9;grid-row:1;background:#fff;container-type:inline-size}.editorial-panel{box-sizing:border-box;width:100%;min-height:145cqi;padding:clamp(12px,5cqi,20px);display:flex;flex-direction:column;gap:clamp(12px,5cqi,20px)}.editorial-header{text-align:center;flex:0 0 auto}.editorial-header>span{display:block;margin-bottom:clamp(5px,2.2cqi,9px);font-size:clamp(7px,3.2cqi,12px);line-height:1.1;color:#68726f}.editorial-header h3{max-width:14ch;margin:0 auto;font:400 clamp(17px,7.5cqi,30px)/.98 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.025em}.editorial-header p{max-width:30ch;margin:clamp(7px,3cqi,12px) auto 0;font-size:clamp(7px,3.2cqi,12px);line-height:1.25}.editorial-list{display:grid;row-gap:clamp(4px,2cqi,8px)}.editorial-item{min-height:clamp(62px,28cqi,108px);padding:clamp(10px,4.5cqi,18px) 0;border-bottom:1px solid #dfe7e4;display:grid;grid-template-columns:clamp(48px,22cqi,84px) minmax(0,1fr);align-items:center;gap:clamp(12px,5cqi,20px);color:var(--ink)}.editorial-item img{display:block;width:100%;aspect-ratio:1;height:auto;border-radius:clamp(10px,4.5cqi,18px);object-fit:cover}.editorial-item__copy{min-width:0;align-self:stretch;display:flex;flex-direction:column;justify-content:center}.editorial-item strong{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font:italic 400 clamp(11px,5.2cqi,20px)/1 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.015em}.editorial-item__body{display:-webkit-box;margin-top:clamp(4px,2cqi,8px);overflow:hidden;-webkit-box-orient:vertical;-webkit-line-clamp:2;font-size:clamp(9px,3.6cqi,13px);line-height:1.25}.editorial-item__link{align-self:flex-start;margin-top:clamp(7px,3cqi,12px);padding-bottom:2px;border-bottom:1px solid currentColor;color:#006b58;font-size:clamp(9px,3.6cqi,13px);line-height:1.15}.editorial-button{box-sizing:border-box;min-height:clamp(30px,12cqi,46px);margin-top:clamp(4px,2cqi,8px);border:1px solid #006b58;border-radius:clamp(8px,4cqi,16px);display:flex;align-items:center;justify-content:center;color:#006b58;font-size:clamp(8px,3.8cqi,14px);line-height:1}
  .tile--product-grid{grid-column:9/13;grid-row:1;box-sizing:border-box;background:var(--tint);padding:clamp(10px,.83vw,12px);display:grid;grid-template-rows:auto auto minmax(0,1fr) auto;gap:clamp(6px,.49vw,8px);container-type:inline-size}.product-grid__header{text-align:center}.product-grid__header h3{max-width:24ch;margin:0 auto;font:400 clamp(14px,3.8cqi,22px)/1 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.02em}.product-grid__header p{margin:clamp(3px,.28vw,4px) 0 0;font-size:clamp(8px,1.9cqi,11px);line-height:1.2}.product-grid__tabs{min-width:0;border-bottom:1px solid rgba(0,0,0,.12);display:flex;align-items:end;gap:clamp(6px,2.2cqi,13px);overflow:hidden;white-space:nowrap}.product-grid__tabs span{height:clamp(17px,4.5cqi,26px);display:flex;align-items:center;border-bottom:2px solid transparent;font-size:clamp(7px,1.8cqi,10px);line-height:1}.product-grid__tabs .active{border-color:#005c47}.product-grid__cards{min-height:0;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));align-items:end;gap:clamp(5px,1.7cqi,10px)}.product-grid__card{position:relative;min-width:0;height:100%;margin:0;display:grid;grid-template-rows:minmax(0,1fr) auto auto;gap:clamp(2px,.7cqi,4px);align-items:end}.product-grid__card img{display:block;width:100%;height:100%;min-height:0;border-radius:clamp(5px,1.8cqi,10px);object-fit:cover}.product-grid__card figcaption{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font:italic 400 clamp(8px,2.3cqi,13px)/1 "Amica Rhymes",var(--font-serif,Georgia,serif)}.product-grid__offer{position:absolute;z-index:1;top:0;left:0;padding:2px 3px;border-radius:2px;background:#6cf5e2;font:400 clamp(4px,1.2cqi,7px)/1 var(--font-mono,ui-monospace,monospace);text-transform:uppercase}.product-grid__arrow{justify-self:end;width:clamp(16px,4cqi,23px);height:clamp(16px,4cqi,23px);border-radius:50%;background:#003d33;color:#fff;display:grid;place-items:center;font-size:clamp(9px,2.3cqi,13px);line-height:1}.product-grid__button{justify-self:center;min-height:clamp(23px,5.5cqi,32px);box-sizing:border-box;padding:0 clamp(9px,2.8cqi,16px);border:1px solid #005c47;border-radius:clamp(5px,1.4cqi,8px);display:flex;align-items:center;color:#005c47;font-size:clamp(8px,2cqi,12px);line-height:1}
  .tile--stat{grid-column:7/11;grid-row:2;background:var(--forest);color:var(--cream);padding:var(--tile-padding-standard);display:flex;flex-direction:column;justify-content:center}.tile--stat strong{font:400 clamp(3rem,5vw,4.5rem)/.9 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.07em}.tile--stat p{margin:9px 0 0;font-size:12px;color:rgba(247,244,232,.7)}
  .tile--hero-copy{grid-column:1/3;grid-row:2;background:var(--tint)}.hero-copy{box-sizing:border-box;width:100%;height:100%;padding:var(--tile-padding-standard);display:flex;flex-direction:column}.hero-top{display:flex;justify-content:space-between;align-items:center}.logo{display:block;width:clamp(60px,6.94vw,100px);color:var(--forest)}.logo :global(svg){display:block;width:100%;height:auto}.logo :global(path){fill:currentColor}.login{border:1px solid #003d33;border-radius:6px;padding:0 8px;font-size:clamp(9px,.9vw,13px);color:#005c47;font-weight:400;height:30px;display:flex;align-items:center;font-family:"Apercu Pro",var(--font-primary,system-ui);background:transparent}.hero-content{margin:auto 0}.hero-content h2{margin:0;font:400 clamp(20px,1.95vw,28px)/1.02 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.02em}.hero-content em{color:var(--teal);font-family:"Amica Rhymes",var(--font-serif,Georgia,serif);font-style:italic}.hero-content p{margin:clamp(6px,.56vw,8px) 0;font-size:clamp(8px,.83vw,12px);line-height:1.25;color:color-mix(in srgb,var(--ink) 70%,transparent)}.hero-content .button{height:clamp(28px,2.5vw,36px);border-radius:8px;padding:0 12px;font-size:clamp(9px,.83vw,12px)}.tile--hero-data{grid-column:3/7;grid-row:2;background:var(--forest)}.hero-data{box-sizing:border-box;width:100%;height:100%;padding:var(--tile-padding-standard);color:var(--cream);display:flex;flex-direction:column;justify-content:space-between}.hero-data__eyebrow{font-size:clamp(9px,.85vw,12px);font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:#00dbba}.hero-data dl{margin:0;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(9px,.83vw,12px)}.hero-data dl div{display:flex;flex-direction:column;gap:3px;padding-right:clamp(8px,.83vw,12px);border-right:1px solid rgba(214,252,245,.25)}.hero-data dl div:last-child{border-right:0}.hero-data dt{font:400 clamp(24px,2.5vw,36px)/1 "Amica Rhymes",var(--font-serif,Georgia,serif)}.hero-data dd{margin:0;font-size:clamp(9px,.85vw,12px);color:rgba(247,244,232,.72)}.hero-data__status{font-size:clamp(9px,.75vw,11px);color:#00dbba}
  .tile--faq{grid-column:11/13;grid-row:2;background:#fff;padding:var(--tile-padding-standard);display:flex;flex-direction:column}.faq-row{flex:1;border-bottom:1px solid #e5efec;display:flex;align-items:center;gap:12px;justify-content:space-between;font-size:clamp(10px,1.04vw,15px);line-height:1.2;padding:8px 0}.faq-row:last-child{border-bottom:none}.faq-row i{display:inline-flex;width:18px;flex-shrink:0}.faq-row i :global(svg){width:100%}
  .tile--nav{grid-column:1/3;grid-row:3;background:var(--forest);padding:var(--tile-padding-compact);display:flex;align-items:center}.nav-card{width:100%;box-sizing:border-box;border-radius:clamp(9px,.83vw,12px);background:#fff;overflow:hidden;padding:clamp(6px,.56vw,8px) clamp(10px,.97vw,14px)}.nav-row{display:flex;align-items:center;justify-content:space-between;min-height:clamp(25px,2.36vw,34px);border-bottom:1px solid #e5efec;font-size:clamp(8px,.903vw,13px)}.nav-row i{display:grid;place-items:center;width:clamp(15px,1.25vw,18px);height:clamp(15px,1.25vw,18px);border-radius:50%;background:var(--mint);font-size:clamp(8px,.76vw,11px);color:var(--forest);font-style:normal}
  .tile--macro{grid-column:7/9;grid-row:3;box-sizing:border-box;background:var(--tint);padding:16px;display:flex;align-items:center;justify-content:center;container-type:inline-size}.macro-ring{border:2px solid #00a88f;outline:2px solid #00a88f;outline-offset:5px;border-radius:30px;background:#00a88f;width:100%;height:112px;color:#000000;display:flex;align-items:center;justify-content:center;padding:0 24px;font-size:clamp(16px,7.5cqi,30px);font-weight:400;white-space:nowrap;font-family:"Apercu Pro",var(--font-primary,system-ui)}
  .tile--picker{grid-column:3/7;grid-row:3;background:#fff;padding:var(--tile-padding-standard)}.picker-shell{width:100%;height:100%;display:flex;flex-direction:column;justify-content:center;gap:clamp(16px,1.39vw,20px)}.picker-shell h3{margin:0;font:400 clamp(16px,1.67vw,24px)/1.05 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.025em;text-align:center}.picker-row{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));align-items:center;gap:clamp(8px,.7vw,10px);width:100%}.product-card{--product-padding:clamp(5px,.49vw,7px);position:relative;box-sizing:border-box;min-width:0;aspect-ratio:1;border:1px solid #c7d8d3;border-radius:clamp(7px,.83vw,12px);padding:var(--product-padding);display:flex;flex-direction:column;align-items:center;background:#fff;box-shadow:0 1px 7px rgba(0,133,107,.12)}.product-card--selected{border:2px solid var(--teal);padding:calc(var(--product-padding) - 1px)}.checkbox{position:absolute;right:clamp(5px,.49vw,7px);top:clamp(5px,.49vw,7px);width:clamp(11px,.97vw,14px);height:clamp(11px,.97vw,14px);border:1px solid #005c47;border-radius:clamp(2px,.28vw,4px);font-family:Arial,sans-serif;font-size:clamp(7px,.63vw,9px);font-style:normal;font-weight:700;line-height:1;display:grid;place-items:center}.product-card--selected .checkbox{background:var(--teal);border-color:var(--teal);color:#fff}.product-illustration{width:100%;min-height:0;flex:1;overflow:hidden;display:flex;align-items:flex-end;justify-content:center;margin-top:clamp(9px,.83vw,12px)}.product-illustration :global(svg){display:block;width:100%;height:auto;max-height:100%;object-fit:contain;object-position:center bottom}.product-card[data-product="condo"] .product-illustration :global(svg){transform:scale(1.9);transform-origin:center bottom}.product-name{margin-top:clamp(3px,.28vw,4px);font-size:clamp(9px,.8vw,12px);line-height:1.1;color:var(--ink)}
  .tile--testimonial{grid-column:9/13;grid-row:3;background:var(--forest);color:var(--cream);padding:var(--tile-padding-standard);display:flex;flex-direction:column}.quote-mark{font:400 56px/.8 "Amica Rhymes",var(--font-serif,Georgia,serif);color:var(--teal);display:block}.tile--testimonial p{font-size:19px;line-height:1.35;margin:0 0 auto;font-family:"Amica Rhymes",var(--font-serif,Georgia,serif);font-weight:400;color:var(--cream)}.tile--testimonial em{color:#00dbba;font-family:"Amica Rhymes",var(--font-serif,Georgia,serif);font-style:italic}.attribution-row{display:flex;align-items:center;gap:clamp(10px,.97vw,14px);margin-top:16px}.avatar{width:clamp(34px,3.05vw,44px);height:clamp(34px,3.05vw,44px);border-radius:50%;object-fit:cover;flex-shrink:0}.attribution-info{display:flex;flex-direction:column;gap:2px}.attribution-name{font-family:"Apercu Pro",var(--font-primary,system-ui);font-size:clamp(12px,1.18vw,17px);line-height:1.1;color:var(--cream);font-weight:400}.attribution-subtext{font-family:"Apercu Pro",var(--font-primary,system-ui);font-size:clamp(10px,.97vw,14px);line-height:1.15;color:rgba(247,244,232,.6)}
  .tile--hero-copy{grid-column:5/7}
  .tile--masthead{grid-column:1/5;grid-row:2}
  .tile--masthead .masthead-card{width:44%}
  .tile--stats{grid-column:5/7;grid-row:2;box-sizing:border-box;padding:clamp(10px,.83vw,12px);background:var(--tint);container-type:inline-size;display:flex;flex-direction:column;gap:clamp(8px,4cqi,14px)}.stats-header{box-sizing:border-box;width:100%;padding:0 clamp(10px,5cqi,18px);text-align:center}.stats-header>span{display:block;color:#6e6e79;font-size:clamp(5px,2.5cqi,8px);line-height:1.2}.stats-header h3{max-width:24ch;margin:clamp(4px,2cqi,7px) auto 0;font:400 clamp(19px,9cqi,30px)/1.05 "Amica Rhymes",var(--font-serif,Georgia,serif);letter-spacing:-.02em}.stats-header p{max-width:34ch;margin:clamp(4px,2cqi,7px) auto 0;font-size:clamp(6px,2.8cqi,9px);line-height:1.25}.stats-card{box-sizing:border-box;width:100%;flex:1;padding:clamp(10px,5cqi,18px);border-radius:clamp(8px,4cqi,14px);background:#fff;box-shadow:0 1px 12px rgba(0,133,107,.15);display:flex;flex-direction:column;justify-content:center}.stats-card__group{display:flex;flex-direction:column}.stats-card__stat{min-width:0;padding:clamp(6px,3cqi,12px) 0;border-bottom:1px solid rgba(0,0,0,.1);display:flex;flex-direction:column;align-items:center;gap:clamp(2px,1cqi,4px);text-align:center}.stats-card__stat:last-child{border-bottom:0}.stats-card__stat strong{color:#00a88f;font:200 clamp(20px,10cqi,34px)/1 "Apercu Pro",var(--font-primary,system-ui);letter-spacing:-.04em;white-space:nowrap}.stats-card__stat small{font-size:.68em;font-weight:200;vertical-align:top}.stats-card__stat>span{color:#003d33;font-size:clamp(9px,4cqi,13px);line-height:1.25;text-align:center}
  .tile--testimonial{grid-column:7/11;grid-row:2}
  .tile--testimonial{padding:clamp(14px,1.11vw,16px)}
  .tile--testimonial .quote-mark{font-size:clamp(38px,3.2vw,46px)}
  .tile--testimonial p{font-size:clamp(24px,2.25vw,32px);line-height:1.08}
  .tile--testimonial .attribution-row{margin-top:10px}
  .tile--hero-copy{grid-column:9/13;grid-row:3}
  @media (max-width:960px){
    .mosaic{overflow:hidden}
    .mosaic__grid{grid-template-columns:repeat(2,minmax(0,1fr));grid-template-rows:none;aspect-ratio:auto}
    .tile{grid-row:auto;aspect-ratio:1}
    .tile--photo,.tile--product-grid,.tile--masthead,.tile--testimonial,.tile--picker,.tile--hero-copy{grid-column:span 2;aspect-ratio:2}
    .tile--quick-links,.tile--editorial,.tile--stats,.tile--faq,.tile--nav,.tile--macro{grid-column:span 1}
    .tile--quick-links{order:1}.tile--editorial{order:2}.tile--photo{order:3}.tile--product-grid{order:4}
    .tile--masthead{order:5}.tile--stats{order:6}.tile--faq{order:7}.tile--testimonial{order:8}
    .tile--nav{order:9}.tile--macro{order:10}.tile--picker{order:11}.tile--hero-copy{order:12}
    .tile--product-grid,.tile--stats,.tile--nav,.tile--picker{container-type:inline-size}
    .tile--nav{padding:clamp(10px,5cqi,20px)}
    .tile--picker{padding:clamp(16px,4cqi,24px)}
    .tile--faq{padding:clamp(12px,4vw,24px)}
    .tile--stats{gap:4px}
    .tile--stats .stats-header{padding:0 6px}
    .tile--stats .stats-header h3{font-size:clamp(13px,7cqi,18px);margin-top:2px}
    .tile--stats .stats-card{padding:8px}
    .tile--stats .stats-card__stat{padding:3px 0;gap:1px}
    .tile--stats .stats-card__stat strong{font-size:clamp(15px,7cqi,20px)}
    .tile--stats .stats-card__stat>span{font-size:clamp(7px,3cqi,9px);line-height:1.15}
  }
</style>
