---

---

<div class="about-modal" id="about-modal" role="dialog" aria-modal="true" aria-labelledby="about-modal-title" hidden>
  <div class="about-backdrop" data-about-close></div>
  <div class="about-content" tabindex="-1">
    <button class="about-close" data-about-close aria-label="Close about panel">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <path d="M18 6L6 18M6 6l12 12" />
      </svg>
    </button>

    <div class="about-body">
      <h2 id="about-modal-title">Sam Stringer-Hye</h2>

      <p class="about-bio">
        Experienced Product and UX/UI designer with 10+ years of crafting intuitive, high-performing digital experiences for top brands. Skilled in designing scalable systems, motion design, and seamless user interactions that bridge creativity and functionality.
      </p>

      <p class="about-bio">
        Adept at collaborating with cross-functional teams and stakeholders to bring user-centered solutions to life. Passionate about refining and elevating digital experiences through thoughtful design and problem-solving.
      </p>

      <nav class="about-links" aria-label="Contact links">
        <a href="mailto:sam@samstringerhye.com" class="about-link">sam@samstringerhye.com</a>
        <a href="https://linkedin.com/in/samstringerhye" target="_blank" rel="noopener noreferrer" class="about-link">LinkedIn<span class="visually-hidden"> (opens in new tab)</span></a>
        <!-- TODO: Add resume PDF to /assets/resume.pdf -->
        <a href="/assets/resume.pdf" download class="about-link">Download Resume</a>
      </nav>
    </div>
  </div>
</div>

<style>
  .about-modal {
    position: fixed;
    inset: 0;
    z-index: var(--z-modal);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .about-modal[hidden] {
    display: none;
  }

  .about-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
  }

  .about-content {
    position: relative;
    width: 100%;
    max-width: 520px;
    max-height: 85vh;
    background: var(--color-bg-primary);
    padding: var(--space-8) var(--space-6);
    overflow-y: auto;
    border-radius: var(--radius-lg);
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.12);
  }

  .about-close {
    position: absolute;
    top: var(--space-3);
    right: var(--space-3);
    color: var(--color-text-secondary);
    transition: color 0.2s ease;
  }

  .about-close:hover {
    color: var(--color-text-primary);
  }

  .about-body {
    padding-top: var(--space-6);
  }

  .about-body h2 {
    margin-bottom: var(--space-3);
  }

  .about-bio {
    line-height: var(--line-height-body);
    margin-bottom: var(--space-4);
  }

  .about-bio:last-of-type {
    margin-bottom: var(--space-6);
  }

  .about-links {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  @media (max-width: 768px) {
    .about-content {
      max-width: 100%;
    }
  }
</style>
