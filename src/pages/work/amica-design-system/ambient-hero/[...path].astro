---
// This route exists only so Astro's router recognizes /work/amica-design-system/ambient-hero/*
// as a valid SSR path and invokes middleware first. Middleware always intercepts requests
// under this prefix (see src/middleware.ts, serveEmbeddedAsset) and returns the embedded
// ambient-hero demo's static files directly — this component's own render path is never reached.
export const prerender = false
---
<html>
  <body>Redirecting…</body>
</html>
